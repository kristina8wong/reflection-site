{
  "name": "year-reflection-functions",
  "description": "Cloud Functions for Year Reflection (sync deleted users)",
  "main": "index.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0"
  },
  "private": true
}
